RewriteEngine On
RewriteBase /

# Если файл или папка существуют - используем их напрямую
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Все остальное перенаправляем на index.php
RewriteRule ^(.*)$ index.php?url=$1 [L,QSA]